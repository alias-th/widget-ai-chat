import React, { ReactNode } from "react";
type AnimatedElementProps = {
    element: ReactNode[];
    gap?: string;
};
export declare const AnimatedElement: (props: AnimatedElementProps) => React.JSX.Element;
export {};

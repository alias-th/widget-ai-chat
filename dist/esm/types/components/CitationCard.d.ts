import React from "react";
import { CitationType } from "../types/chats";
type CitationCardProps = {
    citation: CitationType;
};
export declare const CitationCard: (props: CitationCardProps) => React.JSX.Element;
export {};
